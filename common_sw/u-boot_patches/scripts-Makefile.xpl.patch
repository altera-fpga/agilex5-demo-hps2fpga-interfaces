diff --git a/scripts/Makefile.xpl b/scripts/Makefile.xpl
index 52f014ad..50934645 100644
--- a/scripts/Makefile.xpl
+++ b/scripts/Makefile.xpl
@@ -508,7 +508,7 @@ quiet_cmd_u-boot-spl ?= LTO     $@
       cmd_u-boot-spl ?= \
 	(									\
 		cd $(obj) &&							\
-		touch $(patsubst $(obj)/%,%,$(u-boot-spl-main)) &&		\
+		for f in $(patsubst $(obj)/%,%,$(u-boot-spl-main)); do [ -s "$$f" ] || { rm -f "$$f"; $(AR) rcs "$$f"; }; done &&	\
 		$(CC) -nostdlib -nostartfiles $(LTO_FINAL_LDFLAGS) $(c_flags)	\
 		$(KBUILD_LDFLAGS:%=-Wl,%) $(LDFLAGS_$(@F):%=-Wl,%)		\
 		$(patsubst $(obj)/%,%,$(u-boot-spl-init))			\
@@ -525,7 +525,7 @@ quiet_cmd_u-boot-spl ?= LD      $@
       cmd_u-boot-spl ?= \
 	(								\
 		cd $(obj) &&						\
-		touch $(patsubst $(obj)/%,%,$(u-boot-spl-main)) &&	\
+		for f in $(patsubst $(obj)/%,%,$(u-boot-spl-main)); do [ -s "$$f" ] || { rm -f "$$f"; $(AR) rcs "$$f"; }; done &&	\
 		$(LD) $(KBUILD_LDFLAGS) $(LDFLAGS_$(@F))		\
 		$(patsubst $(obj)/%,%,$(u-boot-spl-init))		\
 		--whole-archive						\
